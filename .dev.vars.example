# Placeholders only. Copy to .dev.vars (ignored by Git), then replace every value.
GOOGLE_CLIENT_ID="REPLACE_WITH_WEB_OAUTH_CLIENT_ID"
GOOGLE_CLIENT_SECRET="REPLACE_WITH_WEB_OAUTH_CLIENT_SECRET"
ADMIN_PASSWORD="REPLACE_WITH_RANDOM_PASSWORD_OF_AT_LEAST_32_CHARACTERS"
TOKEN_ENCRYPTION_KEY="REPLACE_WITH_BASE64URL_ENCODED_32_RANDOM_BYTES"
# Must exactly match the origin in the browser. Use HTTPS in production.
SITE_ORIGIN="http://localhost:3123"
